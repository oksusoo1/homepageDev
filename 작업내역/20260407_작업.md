# 2026-04-07 작업 내역

## 1. CLAUDE.md 개발 규칙 추가
- `작업내역/` 폴더의 최신 파일을 작업 전 반드시 읽도록 규칙 추가

---

## 2. 고객 포털 결제 탭 추가

### 수정
- `app/my/[subdomain]/page.js`
  - TABS에 `'결제'` 추가 (인덱스 3)
  - `paymentMethod`, `billingHistory` state 추가
  - 데이터 fetch: `customer_payment_methods` (is_active, is_default), `billing_history`
  - 구독 조회 `.single()` → `.maybeSingle()` (406 에러 방지)

### 결제 탭 구성 (3개 카드)
1. **구독 현황** — 상태/월구독료/결제방식/다음결제일
2. **등록된 카드** — 카드명 + 끝 4자리 / 없으면 등록 유도
3. **결제 내역** — 기간별 목록, 납부완료/미납/연체 색상 구분

---

## 3. 카드 등록 버그 수정

### 수정
- `app/payment/card/page.js`
  - **버그**: `upsert({ onConflict: 'customer_id' })` → `customer_id`에 unique constraint 없어서 400 Bad Request
  - **수정**: 기존 카드 `is_active=false` update 후 신규 insert로 변경
  - 구독 조회 `.single()` → `.maybeSingle()`
  - 디버깅용 `console.log` 추가 (각 단계별 결과 및 에러 로깅)

- `app/my/[subdomain]/page.js`
  - 구독 조회 `.single()` → `.maybeSingle()`

---

## 4. 무료 체험 14일 + 배포 플로우 구현

### DB 변경 (Supabase SQL 실행 필요)
```sql
ALTER TABLE sites ADD COLUMN IF NOT EXISTS trial_started_at TIMESTAMP;
ALTER TABLE sites ADD COLUMN IF NOT EXISTS trial_ends_at TIMESTAMP;

ALTER TABLE subscriptions DROP CONSTRAINT IF EXISTS subscriptions_status_check;
ALTER TABLE subscriptions ADD CONSTRAINT subscriptions_status_check
  CHECK (status IN ('trial', 'active', 'paused', 'cancelled'));
```

### 배포 시 체험 자동 시작
- `app/my/[subdomain]/page.js` — `deploySite()` 수정
- `app/editor/[subdomain]/page.js` — `deploySite()` 수정
- 배포 클릭 시:
  - `sites.trial_started_at = 현재`
  - `sites.trial_ends_at = 현재 + 14일`
  - 구독 없으면 자동 생성 (`status: 'trial'`)
  - 구독 있으면 `status: 'trial'` 업데이트
  - `next_billing_date = 체험 만료 익월 1일`

### suspended 사이트 안내 페이지
- `app/preview/[domain]/page.js`
  - `getSite()`: `.in('status', ['published', 'draft'])` → `.neq('status', 'cancelled')` (suspended 포함)
  - `sites.status === 'suspended'` 이면 🔒 안내 페이지 렌더링

### 관리자 콘솔 체험 현황
- `app/platform/page.js`
  - 사이트 목록 테이블에 `체험현황` 컬럼 추가
  - `체험중 D-N` (노란색) / `만료 N일 초과` (빨간색) / `체험 없음` (회색) 뱃지 표시

### 결제 탭 체험 정보 표시
- `app/my/[subdomain]/page.js`
  - 구독 현황 카드에 체험 안내 배너 추가 (노란 박스)
  - `체험 만료일` + `D-N` 표시
  - 상태 텍스트: trial → `🟡 무료 체험중`

---

## 수정된 파일 전체 목록

| 파일 | 작업 |
|------|------|
| `CLAUDE.md` | 개발 규칙 — 작업내역 참고 규칙 추가 |
| `app/my/[subdomain]/page.js` | 결제 탭 추가, deploySite() 체험 처리, 결제탭 체험 표시 |
| `app/editor/[subdomain]/page.js` | deploySite() 체험 처리 |
| `app/payment/card/page.js` | upsert→insert 버그 수정, 로그 추가 |
| `app/preview/[domain]/page.js` | suspended 안내 페이지 |
| `app/platform/page.js` | 사이트 목록 체험 D-day 컬럼 추가 |

---

## 다음 작업 후보
- 체험 만료 후 자동 정지 처리 (배치/스케줄러 또는 관리자 수동)
- 카드 등록 후 subscriptions.status = 'active' 전환 로직
- console.log 디버깅 코드 제거 (실서비스 전)
- 관리자 콘솔 — 체험 만료 임박 알림 (D-3 이하 강조 등)
