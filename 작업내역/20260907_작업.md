# 2026-09-07 작업 내역

## 1. 공개 사이트 상태 리본
- `SiteStatusRibbon` — 우측 상단 대각선 컬러 리본
- `app/s/[siteCode]/layout.js` — 홈/게시판/문의 공통 표시 (`/admin` 제외)

## 2. 주체 용어 · 테이블 사상
- **customers** = 고객 · **staff** = 직원 · **user** = 사용자(방문자)
- 공개 사이트만 `user_` 접두: `user_posts`, `user_messages`

## 3. DB
- `007_user_messages.sql` — `user_messages` 신설
- `008_rename_posts_to_user_posts.sql` — `posts` → `user_posts`
- 스키마·샘플 동기화

## 4. 문의 분리
| 구분 | 테이블 | UI |
|------|--------|-----|
| 사용자→고객 | `user_messages` | `/s/.../contact` 목록·등록 · `/my` 방문자 문의 답변 |
| 고객→직원 수정 | `support_tickets` | `/my` 수정요청 |
| 고객→직원 제작 | `inquiries` | `/my` 제작문의 |

- migration `009`: `is_private`, `reply_content`, `replied_at`, status `new|replied|done`

## 7. 공통코드
- `/platform` → **공통코드** 메뉴 (`PlatformCommonCodes`)
- DB: `code_groups` · `common_codes` (`011_common_codes.sql`)
- 추가 시드: `012_common_codes_extra.sql` (`BILLING_STATUS`, `TICKET_PRIORITY`, `BUSINESS_TYPE`)
- 표시: `lib/common-codes.js` (`codeLabel` / `codeColor`)
- 적용 범위: `/platform` 목록·상세, `/my`, `/my/[subdomain]`, `/s/[siteCode]/admin` 상태·유형·결제·티켓 라벨

## 8. 사장님 admin left 메뉴
- `/s/[siteCode]/admin` — 상단 탭 → left 사이드바 (상위/하위 펼침)
- `components/SiteAdminShell.js`
- 그룹: 내 사이트 / 소통 / 결제
- 제외(의도): `SiteStatusRibbon` UX 문구(미공개 등) — 공통코드와 문구가 다름
- 상위/하위 메뉴 구조는 추후

## 9. 인증 세션 점검 (`requireAuthUser`)
- 원인: 페이지 진입 시 `getUser()`만 쓰면 로컬 세션 있는데도 `/login`으로 튕기는 경우
- 공통: `lib/auth.js` → `getSession` 우선, 없으면 `getUser`
- 적용: `/my`, `/my/[subdomain]`, `/setup`, `/editor`, `/s/.../admin(+editor·payment)`, `/my/payment/one-time/*`, `/platform`, `ReviewSiteGate`, `site-owner-auth`
- 기존: `/templates`, `getAuthCustomer`/`getAuthStaff` 등
