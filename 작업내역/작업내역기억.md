# 그날의 작업이 완료되면

1. **개발 명세**(플로우·스키마·도식)가 바뀌었으면 → `my-platform/docs/` 에 **Markdown**으로 버전 문서·목록·도식을 갱신  
   (룰: `.cursor/rules/dev-docs-md.mdc`)
2. `CLAUDE.md` 현황이 필요하면 업데이트
3. `작업내역/{날짜}_작업.md` 에는 **짧은 메모 + docs 링크**만 (본문은 docs)
