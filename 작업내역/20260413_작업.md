# 2026-04-13 작업 내역

## 1. 결제 로직 버그 수정

### `lib/deploy.js`
- **버그**: `next_billing_date` 계산 시 `nextBilling.setDate(1)` 로 강제 1일 설정되던 문제 제거
- **수정**: `trial_ends + 1달` 날짜 그대로 유지
  - 예: 4/13 배포 → trial_ends 4/27 → next_billing_date **5/27** (기존엔 5/1)

### `app/api/payment/billing-auth/route.js` (실 Toss 연동)
- 빌링키 발급 후 **즉시 결제 청구** 추가 (Toss 빌링 API 호출)
- `billing_history` — 이번 달 `paid` 레코드 자동 생성
- 재구독 시: `cancelled_at`, `cancels_at` 초기화 + `next_billing_date` = 오늘 + 1달 갱신
- 재구독 시: `sites.status = 'published'` 복원

### `app/payment/card/page.js` (MOCK 모드)
- 카드 등록 시 `billing_history` 이번 달 `paid` 레코드 자동 생성
- 재구독 여부(`cancelled_at` 존재) 판단:
  - 재구독: `next_billing_date` = 오늘 + 1달 갱신, `cancelled_at`/`cancels_at` 초기화, 사이트 `published` 복원
  - 첫 등록: `next_billing_date` 기존 값(trial 기준) 유지

---

## 2. 결제 정책 확정

```
배포 → trial 14일 → 카드 등록(즉시 결제)
next_billing_date = trial_ends_at + 1달 (불변)

예: 4/13 배포 → trial_ends 4/27
    4/20 카드 등록 → 즉시 30,000원 결제 → 다음 결제일 5/27
    4/13 카드 등록 → 즉시 30,000원 결제 → 다음 결제일 5/27 (trial 잔여분 혜택)
```

---

## 3. 구독 해지 / 재구독 구현

### DB 변경 (Supabase SQL 실행 필요)
```sql
ALTER TABLE subscriptions ADD COLUMN IF NOT EXISTS cancels_at TIMESTAMP;

-- 트리거 수정: cancels_at 있을 때 즉시 site 상태 변경 안 함
CREATE OR REPLACE FUNCTION sync_site_status()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.status = 'cancelled' AND NEW.cancels_at IS NULL THEN
    UPDATE sites SET status = 'cancelled', updated_at = NOW() WHERE site_id = NEW.site_id;
  ELSIF NEW.status = 'paused' THEN
    UPDATE sites SET status = 'suspended', updated_at = NOW() WHERE site_id = NEW.site_id;
  ELSIF NEW.status IN ('active', 'trial') THEN
    UPDATE sites SET status = 'published', updated_at = NOW()
    WHERE site_id = NEW.site_id AND status IN ('suspended', 'cancelled');
  END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;
```

### 구독 해지 정책
- 해지 요청 → `subscriptions.cancelled_at = now`, `cancels_at = next_billing_date`, status는 `active` 유지
- 사이트는 `published` 유지 (잔여 기간 계속 이용)
- 관리자가 `cancels_at` 도래 후 수동으로 `cancelled` 처리

### 구독 해지 UI (`app/my/[subdomain]/page.js` 결제 탭)

| 상태 | 표시 | 버튼 |
|------|------|------|
| active (정상) | 이용중 | 구독 해지 |
| active + cancels_at | 해지 예정 (MM.DD까지) | 해지 철회하기 |
| cancelled | 해지됨 | 재구독하기 → |

---

## 4. Trial 재사용 방지

### `lib/deploy.js`
- 4번째 파라미터로 `site` 객체 수신
- `site.trial_started_at` 존재 시 trial 없이 배포만 처리, `requireCard: true` 반환
- 호출부(`my/[subdomain]/page.js`)에서 `requireCard: true`이면 `/payment/card`로 즉시 이동

---

## 5. 회원 탈퇴 구현

### DB 변경 (Supabase SQL 실행 필요)
```sql
ALTER TABLE customers ADD COLUMN IF NOT EXISTS withdraw_at TIMESTAMP;
```

### 탈퇴 정책

| 상태 | 처리 |
|------|------|
| 구독 중 (active + card) | `withdraw_at = next_billing_date` 저장, 구독 `cancels_at` 설정, status 유지 → 만료일까지 이용 가능 |
| Trial 중 / 구독 없음 | 즉시 `withdrawn` + 사이트/구독 `cancelled` |

### 탈퇴 재진입 처리 (`app/my/page.js`)
- `withdraw_at` 미래 → 정상 접근 허용
- `withdraw_at` 과거 → 자동 `withdrawn` 처리 → 재활성화 페이지
- `status = 'withdrawn'` → 재활성화 페이지 표시

### 재활성화 (`app/my/page.js`)
- 탈퇴 고객 로그인 시 재활성화 페이지 표시 (로그아웃 강제 X)
- "계정 재활성화하기" 클릭 → `customers.status = 'active'` → 사이트 목록 로드
- 기존 사이트 데이터 보관 (cancelled 상태), 재구독 후 재운영 가능

### 탈퇴 UI
- 결제 탭 하단 "회원 탈퇴" 섹션
- 2단계 확인: 모달 오픈 → **"탈퇴" 텍스트 직접 입력** 해야 버튼 활성화

---

## 수정된 파일 전체 목록

| 파일 | 작업 |
|------|------|
| `lib/deploy.js` | next_billing_date 버그 수정, trial 재사용 방지, site 파라미터 추가 |
| `app/api/payment/billing-auth/route.js` | 즉시 결제 청구, billing_history 생성, 재구독 처리 |
| `app/payment/card/page.js` | MOCK 즉시 결제, billing_history 생성, 재구독 처리 |
| `app/my/[subdomain]/page.js` | 구독 해지/철회/재구독 UI, trial 재사용 방지, 회원탈퇴 로직/UI, withdrawn 체크 |
| `app/my/page.js` | withdrawn/withdraw_at 체크, 재활성화 UI |
| `app/login/page.js` | withdrawn/withdraw_pending 오류 메시지 추가 |

---

## Supabase SQL 실행 체크리스트

- [ ] `subscriptions.cancels_at` 컬럼 추가
- [ ] `sync_site_status` 트리거 함수 수정
- [ ] `customers.withdraw_at` 컬럼 추가

---

## 다음 작업 후보
- Trial 만료 후 자동 정지 스케줄러 (현재 관리자 수동 처리)
- `payment/card/page.js` 디버깅 `console.log` 제거
- Toss 실결제 SDK 프론트엔드 연동 (`MOCK_MODE = false`)
- 관리자 콘솔: 해지 예정 / 탈퇴 예정 고객 현황 표시
