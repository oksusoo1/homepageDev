# 2026-09-27 작업 — 보안 구조 전환 ⑤ 남은 조회 서버화 + RLS 잠금 SQL

관련: [플로우](../플로우.md) · [테이블명세](../db/테이블명세.md) · [030](../db/sql/migrations/030_rls_lockdown.sql)

## 무엇을
사장님 `/my`·admin·에디터·헤더의 브라우저 `from()` 조회를 Server Action으로 옮김. 030 SQL은 파일만.

## 왜
anon 키로 테이블을 읽으면 잠금 후에도 그대로 열린다. 조회도 서버 권한 검사 뒤에만.

## 영향 범위
- **DB**: 030 파일만 (실행 안 함)
- **본사** `/platform`: 조회는 이미 Action. 헤더 AuthUserBar만 Action으로 프로필
- **고객** `/my` · admin · 에디터 · 결제 화면
- **공개**: 없음 (④)
- **게이트**: 없음

## 주체 UX
사장님 `/my`·관리자 — 화면은 그대로. 목록·결제 정보가 서버에서 온다.

## 0번
- write-actions 컬럼 폴백 제거
- `author_auth_id`: 스키마 dump·022에 없음. 029(`IF NOT EXISTS`)가 유일한 ADD. ④ 작업 중 후반 프로브에서 컬럼이 보였고, 저장소에 ALTER 실행 기록은 없음. 사용자가 029를 실행했거나 SQL 에디터로 넣은 것으로 본다.

## 검증
- `npm run build` 통과 (Next.js 16.1.7)
- E2E S1~S16 통과 · S17 **잠금 대기** — `docs/테스트결과/2026-09-27_0050.md` (`http://127.0.0.1:3006`)
- 030 **미실행** (사용자 실행 대기)

## 030 보강
- lockdown: DO 블록 뒤에 anon/authenticated REVOKE + DEFAULT PRIVILEGES 회수 (`service_role` 유지)
- rollback: GRANT 복원 추가
- `rls_status.sql`: 테이블 RLS·정책 수·권한, 뷰 목록, EXECUTE 가능 함수
- CLAUDE.md: 새 테이블 마이그레이션은 같은 파일에서 RLS ON, 정책 없음. 작성 후 rls_status 확인

## 030 실행 후 검증
- `npm run build` 통과
- E2E S1~S17 전부 통과 — `docs/테스트결과/2026-09-27_0139.md` (`:3007`)
- S17 `lockdown=active` (잠금 대기 아님)

## ⑤ 후속
- 사장님 화면에 빌링키(`pg_customer_id`) 안 내려보냄. 카드 끝자리·이름만
- 공개 게시판 `author_auth_id` 컬럼 폴백 제거
- 서버 조회 error를 빈 목록으로 숨기지 않고 fail 반환
- build 통과 · E2E S1~S17 통과 — `docs/테스트결과/2026-09-27_0154.md`
