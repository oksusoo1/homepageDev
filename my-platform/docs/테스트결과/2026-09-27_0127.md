# E2E 테스트 결과 — 2026-09-27_0127

- 실행 일시: 2026-09-26T16:27:49.649Z (KST 스탬프 2026-09-27_0127)
- 대상 URL: `http://127.0.0.1:3006`
- 브라우저: chromium
- HTML 리포트: `playwright-report/index.html`
- 생성: Playwright onEnd · **이 실행(2026-09-27_0127)의 결과만** (이전 md·메타 재사용 없음)

| # | 시나리오 | 결과 |
|---|---|---|
| S1 | A: 셀프 사이트 생성 → 카드(목업) 등록 → 서비스 시작 | 실패 |
| S2 | A: 체험 중 카드 재등록 | 실패 |
| S3 | 배치 asOf=체험종료일+1 | 실패 |
| S4 | A: 해지 예약 → 배치 asOf=해지예정일+1 → 카드 재구독 | 실패 |
| S5 | 대리: B 의뢰 → staff 견적 → B 선금 입금했어요 → staff 선금 확인 | 실패 |
| S6 | 권한 차단 | 실패 |
| S7 | 공개 범위 (제작 중 비로그인) | 실패 |
| S8 | A: 에디터 내용 저장 | 실패 |
| S9 | 편집 요청에 금지 필드 섞음 | 통과 |
| S10 | /setup 주소 검사 | 실패 |
| S11 | B가 A 사이트 에디터·게시판 관리 접근 | 실패 |
| S12 | 방문자가 공개 문의 게시판에 글 작성 | 실패 |
| S13 | 신규 가입 customers 행 | 실패 |
| S14 | 유료 구독 중 탈퇴 → 배치 확정 | 실패 |
| S15 | 비밀글 열람(비로그인·사장님·작성자) | 실패 |
| S16 | 문의 연락처는 방문자 응답에 없음 | 실패 |
| S17 | DB 직접 접근 차단(anon 키) | 통과 |

## S1. A: 셀프 사이트 생성 → 카드(목업) 등록 → 서비스 시작

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S2. A: 체험 중 카드 재등록

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S3. 배치 asOf=체험종료일+1

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S4. A: 해지 예약 → 배치 asOf=해지예정일+1 → 카드 재구독

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S5. 대리: B 의뢰 → staff 견적 → B 선금 입금했어요 → staff 선금 확인

- 결과: **실패**
- 사이트: `e2e-s5-muim186u`
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S6. 권한 차단

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S7. 공개 범위 (제작 중 비로그인)

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S8. A: 에디터 내용 저장

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S9. 편집 요청에 금지 필드 섞음

- 결과: **통과**
- 검증한 DB 값:
  - `patch_keys` = `content,name`
  - `status_dropped` = `true`

## S10. /setup 주소 검사

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S11. B가 A 사이트 에디터·게시판 관리 접근

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S12. 방문자가 공개 문의 게시판에 글 작성

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S13. 신규 가입 customers 행

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S14. 유료 구독 중 탈퇴 → 배치 확정

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S15. 비밀글 열람(비로그인·사장님·작성자)

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S16. 문의 연락처는 방문자 응답에 없음

- 결과: **실패**
- 에러: TimeoutError: locator.waitFor: Timeout 20000ms exceeded.

## S17. DB 직접 접근 차단(anon 키)

- 결과: **통과**
- 검증한 DB 값:
  - `sites_anon_rows` = `0`
  - `sites_anon_error` = `permission denied for table sites`
  - `sites_rows` = `0`
  - `customers_rows` = `0`
  - `subscriptions_rows` = `0`
  - `billing_history_rows` = `0`
  - `one_time_payments_rows` = `0`
  - `customer_payment_methods_rows` = `0`
  - `staff_rows` = `0`
  - `user_posts_rows` = `0`
  - `support_tickets_rows` = `0`
  - `inquiries_rows` = `0`
  - `lockdown` = `active`
